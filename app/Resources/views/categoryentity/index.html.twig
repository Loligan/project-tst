{% extends 'base.html.twig' %}

{% block body %}
    <h1>Categoryentities list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for categoryEntity in categoryEntities %}
            <tr>
                <td><a href="{{ path('category_show', { 'id': categoryEntity.id }) }}">{{ categoryEntity.id }}</a></td>
                <td>{{ categoryEntity.name }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('category_show', { 'id': categoryEntity.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('category_edit', { 'id': categoryEntity.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('category_new') }}">Create a new categoryEntity</a>
        </li>
    </ul>
{% endblock %}
